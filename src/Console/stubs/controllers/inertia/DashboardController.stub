<?php

namespace {{namespace}};

use App\Http\Controllers\Controller;
use Inertia\Inertia;
use Inertia\Response;

class DashboardController extends Controller
{
    public function index(): Response
    {
        return Inertia::render('{{studlyGuard}}/Dashboard', [
            'auth' => [
                'user' => auth()->guard('{{guard}}')->user(),
            ],
        ]);
    }
}
